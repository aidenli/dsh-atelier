# Atelier 0.3.4

- 更新浮层增加“立即更新”按钮。
- 自动更新和命令行均使用检测到的具体最新版本号，不再使用 `latest`。
- npm 与 GitHub Release 安装命令改为固定版本，避免更新过程中版本漂移。
- 更新成功后提示重启 DSH Web；源码加载模式继续提示编译部署。
- 增加更新失败后的重试保护和重复点击锁。
- 修复 Windows PowerShell UTF-8 发布脚本解析问题及 Git 换行导致的同版本误判。

需要 Node 22.19 或 >=24。更新后请重启 DSH Web。本次发布不调用付费生成接口。
